<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppFavas" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_Primary</item>
        <item name="colorPrimaryVariant">@color/color_Primary_Dark</item>
        <item name="colorOnPrimary">@color/color_Accent</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_Primary</item>
        <item name="colorSecondaryVariant">@color/color_Primary_Dark</item>
        <item name="colorOnSecondary">@color/color_Accent</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.AppFavas.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AppFavas.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AppFavas.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>